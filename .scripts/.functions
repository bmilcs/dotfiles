# 
# bmilcs functions
#

ask_yes_no() {
	echo $1
	while true; do
		read -p "$1 [y/n]: " answer
		case "$answer" in
			[Yy]* ) return 0;;
			[Nn]* ) return 1;;
			* ) echo "Please answer y (yes) or n (no)"
		esac
	done
}
